applicationSettings: {}
sourceConfigurations:
  - name: http/simple
    config:
      ListenAddr: :8080
      MaxBodySize: 51200
relayConfigs:
  - name: memory
    config: {}
distributorConfigurations:
  - name: distributor/performance
    config:
      metricsPollInterval: 60s
